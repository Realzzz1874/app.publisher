export enum ROLES {
  owner = 'owner',
  manager = 'manager',
  guest = 'guest',
}

export enum MESSAGE_STATUS {
  unread = 'unread',
  read = 'read',
}

export enum MESSAGE_CATEGORY {
  member = 'member', // 人员变动【加入 | 移出 | 解散 | 离开】
}
